# Docker compose ports
BACKEND_PORT=8899
FRONTEND_PORT=5174

# Frontend build-time API base:
# - 留空表示同域访问（推荐，Nginx 会反代 /api 到后端）
# - 本地开发可填 http://127.0.0.1:8899
VITE_API_BASE_URL=

# API Key（推荐通过 GitHub Secrets -> STUDIO_HTTP_API_KEY 注入）
# 后端会优先读取此环境变量，不会在配置接口返回明文
STUDIO_HTTP_API_KEY=
